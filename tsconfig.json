{
  "extends": "@quasar/app-vite/tsconfig-preset",
  "compilerOptions": {
    "baseUrl": "."
  },
  "include": [
    "src-electron/installScripts/**/*.ts"  // Use "include" instead of "files" for glob patterns
  ],
  // "files": ["./src-electron/installScripts/*.ts"],
  "exclude": [
    "./dist",
    "./.quasar",
    "./node_modules",
    "./src-capacitor",
    "./src-cordova",
    "./quasar.config.*.temporary.compiled*"
  ]
}